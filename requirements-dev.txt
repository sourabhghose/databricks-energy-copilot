# ============================================================
# AUS Energy Copilot — Development & Test Dependencies
# ============================================================
# Install with:  pip install -r requirements-dev.txt
# ============================================================

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# Async HTTP client (FastAPI ASGI test transport)
httpx>=0.27.0

# Mocking (unittest.mock is stdlib; unittest-mock is the PyPI backport shim)
# The stdlib unittest.mock is used directly in test_agent.py; this entry
# ensures compatibility on older Python versions and with tools that pin it.
unittest-mock>=0.1.0

# Code quality
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0
