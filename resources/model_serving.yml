# ============================================================
# Model Serving Endpoints
# Uncomment after ML models are trained and registered in UC.
# ============================================================

# resources:
#   model_serving_endpoints:
#     price_forecast_endpoint:
#       name: ec-price-forecast
#       config:
#         served_entities:
#           - name: price_forecast_nsw1
#             entity_name: ${var.catalog}.ml.price_forecast_NSW1
#             entity_version: "1"
#             workload_size: Small
#             scale_to_zero_enabled: true
#     anomaly_detection_endpoint:
#       name: ec-anomaly-detection
#       config:
#         served_entities:
#           - name: anomaly_detection_nem
#             entity_name: ${var.catalog}.ml.anomaly_detection_nem
#             entity_version: "1"
#             workload_size: Small
#             scale_to_zero_enabled: true
