# ============================================================
# AUS Energy Copilot — Backend environment template
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.
# ============================================================

# ---- Databricks workspace ----
DATABRICKS_HOST=https://<workspace-id>.azuredatabricks.net
DATABRICKS_TOKEN=dapi<your-personal-access-token>

# ---- Databricks SQL Warehouse ----
# Find in Workspace > SQL Warehouses > (your warehouse) > Connection Details
DATABRICKS_WAREHOUSE_ID=<warehouse-id>

# ---- Unity Catalog ----
# Top-level catalog that contains the silver/gold schemas
DATABRICKS_CATALOG=energy_copilot

# ---- Anthropic (Claude Sonnet 4.5) ----
ANTHROPIC_API_KEY=sk-ant-<your-api-key>

# ---- Lakebase (PostgreSQL-compatible NEM operational DB) ----
LAKEBASE_HOST=<lakebase-endpoint>.postgres.database.azure.com
LAKEBASE_PORT=5432
LAKEBASE_DB=energy_copilot
LAKEBASE_USER=energy_copilot_app
LAKEBASE_PASSWORD=<lakebase-password>

# ---- CORS (comma-separated origins; default "*" allows all in dev) ----
# ALLOW_ORIGINS=https://your-app.azuredatabricks.net

# ---- Log level ----
# LOG_LEVEL=INFO

# ---- Optional: Mosaic AI Agent endpoint ----
# If deploying the agent via Databricks Model Serving, set this URL.
# When set, /api/chat will proxy to this endpoint instead of calling Claude directly.
# AGENT_ENDPOINT_URL=https://<workspace>.azuredatabricks.net/serving-endpoints/energy-copilot-agent/invocations
