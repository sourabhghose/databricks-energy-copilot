# ============================================================
# AUS Energy Copilot — Frontend Environment Variables
# Copy to .env.local for local development.
# DO NOT commit .env.local to version control.
# ============================================================

# Base URL of the FastAPI backend.
# Local dev: http://localhost:8000
# Databricks Apps: set to the deployed App URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# Application display title shown in the browser tab and header.
VITE_APP_TITLE=AUS Energy Copilot

# Set to "true" to serve synthetic mock data from the frontend layer
# without hitting the backend API. Useful for UI-only development.
VITE_ENABLE_MOCK_DATA=false

# Databricks Genie Space ID for the embedded Genie chat widget.
# Find this in the Databricks workspace under AI/BI > Genie Spaces.
# Leave blank to disable the Genie widget.
VITE_GENIE_SPACE_ID=
